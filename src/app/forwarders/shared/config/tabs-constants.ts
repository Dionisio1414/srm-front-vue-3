import type { BaseTabs } from '@/@types/additional';

const FORWARDERS_TABS: BaseTabs[] = [
  { label: 'forwarder.titles.forwarder', to: { name: 'forwarders' } },
  { label: 'supplierMails.title', to: { name: 'supplier-mails' } },
];

export default FORWARDERS_TABS;
